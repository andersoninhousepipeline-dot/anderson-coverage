# Coverage Checker — server configuration (sourced by control scripts)
# Edit values here; all scripts read from this file.

PORT="${PORT:-8100}"            # TCP port to listen on
HOST="0.0.0.0"                  # bind address (0.0.0.0 = all interfaces / LAN)
APP="app.py"                    # Flask entrypoint
PYTHON="${PYTHON:-python3}"     # python interpreter
